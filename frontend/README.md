# Frontend Client (React + Vite)

This directory contains the client-side application for the AI English Scorer.

## Running the app

```bash
# install dependencies
$ npm install

# development server
$ npm run dev

# build for production
$ npm run build
```

> **Note**: For full project documentation and environment setup, please refer to the **Root README** (`../README.md`).
